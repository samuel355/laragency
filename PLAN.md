# BlueGate Realty — Build Plan

Ghana-based real estate platform (Laravel 13 + PostgreSQL + Blade + Leaflet).
Brand: **BlueGate Realty**. Currency: GHS (USD toggle later). Locale: Accra/Greater Accra.

Legend: `[x]` done, `[~]` partial/exists needs work, `[ ]` not started.

Work order: top to bottom. Each item = real DB-backed feature, seeded content, working routes/views — not placeholder markup.

## Public Website

- [x] 1. Home — hero+search, featured, categories, communities, why-us, stats, recently sold, latest listings, investment ops, services, testimonials, partner banks, blog, contact CTA
- [~] 2. About Us — story, mission, vision, values, history, timeline, awards, certs, licenses, management message, CSR, partners
- [~] 3. Leadership & Team — board/execs/agents/surveyors/architects/engineers/legal/property managers/customer service + profile pages
- [~] 4. Property Listing — advanced filters (buy/rent/lease/land/type/location/price/beds/baths/area/status/featured/new) + grid/list/map views
- [~] 5. Property Details — overview, gallery/video/tour/drone/floor plans, interactive map+boundary, info, land details, features, nearby places, agent info, mortgage calc, schedule viewing, request info, similar properties
- [ ] 6. Property Categories (lands/houses/apartments/commercial/luxury/rentals/short stay/agricultural)
- [ ] 7. New Developments (estate projects/apartments/smart cities/gated communities)
- [ ] 8. Featured Properties page
- [ ] 9. Luxury Collection page
- [ ] 10. Recently Sold page
- [ ] 11. Investment Opportunities page
- [~] 12. Services + 13. Service Detail (description/process/benefits/FAQs/contact)
- [ ] 14. Estates (master plan/available plots/plot map/amenities/pricing/gallery/progress)
- [ ] 15. Construction Projects
- [ ] 16. Case Studies
- [ ] 17. Testimonials / 18. Success Stories / 19. Corporate Clients
- [ ] 20. Blog (buying/selling/investment/documentation/law/market trends)
- [ ] 21. News
- [~] 22. FAQs
- [ ] 23. Download Center (brochures/site plans/price lists/company profile/forms)
- [~] 24. Contact Us (offices/map/form/WhatsApp/phone/email)
- [ ] 25. Book Site Visit
- [ ] 26. Request Callback
- [ ] 27. Mortgage Inquiry
- [ ] 28. Property Valuation Request
- [ ] 29. Sell Your Property (photos/docs/price/location upload)
- [ ] 30. Become an Agent

## Authentication
- [~] Login / Register / Forgot / Reset password (roles: client, agent, admin) — admin login only, styled with real template markup; Register/Forgot/Reset and client/agent roles not started

## Client Dashboard
- [ ] Saved/favorite properties, recently viewed, site visit bookings, mortgage applications, document downloads, inquiries, notifications, profile, settings

## Agent Dashboard
- [ ] My listings, leads, client mgmt, appointments/calendar, sales performance, commissions, property requests, messaging, reports

## Admin Dashboard
- [~] Dashboard KPIs (revenue/leads/sales/property/booking/user stats) — real dashboard now live (KPI cards, weekly leads chart, recent-activity feed) at /admin/dashboard; missing agent-performance/conversion-rate style KPIs
- [~] Property management (categories/features/locations/amenities/pricing/media/floor plans/coordinate polygons/documents)
- [ ] Estate management (estates/phases/blocks/plots/availability)
- [~] Interactive mapping (draw polygons/GPS/parcel boundaries — base exists via Leaflet+geometry, needs draw tool + GIS layers)
- [~] CRM (buyers/sellers/leads/follow-ups/notes/history) — Requests page shows all inquiries/site-visits/mortgage-apps as leads with contact actions; still needs notes/follow-up history, not just view+delete
- [~] Bookings (site visits/calendar/agent assignment) — visible in Requests page; no calendar view or agent-assignment UI yet
- [~] Payments (deposits/installments/receipts/invoices — Paystack orders exist, needs installments+receipts) — now has a real admin ledger view (/admin/orders) with collected/paid/pending totals
- [ ] Documents (site plans/survey plans/titles/agreements/brochures)
- [ ] Team management (agents/surveyors/lawyers/roles/permissions — RBAC)
- [~] Website CMS (homepage/pages/menus/banners/testimonials/blog/news/FAQ)
- [ ] Reports (sales/property performance/lead conversion/agent performance/revenue)
- [ ] Marketing (email/SMS/push/featured listings/promo banners)
- [ ] Settings (company profile/branches/SEO/social/payment gateways/maps API/email/security/backups)

## Premium Features (woven in as we go)
- [~] GIS polygon maps (parcels) — needs admin draw tool
- [ ] Compare properties side-by-side
- [ ] AI-powered recommendations (rule-based similarity engine)
- [ ] 360° virtual tours
- [~] Mortgage/affordability calculator
- [~] Real-time availability tracking (parcel status)
- [ ] Installment payment plans
- [ ] Customer document portal
- [ ] Live chat / WhatsApp integration
- [ ] Multi-language / multi-currency
- [ ] Role-based access (admin/agent/client)

---
### Current session log
- Audited existing scaffold: PageController, ListingController, ParcelController, TeamController, CheckoutController, PaymentController, Admin*Controller (content/listings/services/team/faqs/parcels), Blade views under resources/views, Postgres DB migrated + seeded.
- Item 1 (Home) DONE: added Community, Testimonial, PartnerBank, CompanyStat, BlogPost models+migrations+seed data; extended PropertyListing (property_type, property_code, community_id, team_member_id, garages/floors/year_built, video/tour/floor-plan/nearby_places, is_investment/is_new/sold_at); rebuilt PageController@home and home.blade.php with all 14 spec sections; extended ListingController filters (type/property_type/city/price/beds/baths/area/status/featured/investment/new/sort); as dependencies, also built out Service Detail (#13), Blog index+show (#20), Book Site Visit (#25), Mortgage Inquiry incl. affordability calculator (#27) since Home links to them; added site-wide footer + WhatsApp floating button + Resources nav dropdown (previously no footer existed at all). Verified all routes return 200 via local `php artisan serve`.
- Fixed two real rendering bugs found via browser check (Playwright), reported by user as "home page not showing anything": (1) the `light` template's `initRenstate()` script only reveals `#main` (default `opacity:0`) inside a `.fadeOut()` callback on `.loader-wrap` — that element was missing entirely from our Blade layout, so the callback (and the page) never appeared. Restored the loader markup. (2) `.main-footer` in the base template CSS is `position:fixed; bottom:0` (a scroll-reveal trick from the original demo), which pinned the footer over the hero/content at normal viewport heights — overridden to `position:relative` in app-blue.css. Also replaced the header/footer logo `<img>` (baked-in "RENTSTATE" text in the PNG) with a CSS text wordmark reading "BlueGate Realty". Verified with a full Playwright pass: opacity=1, footer at correct scroll position (~8046px), zero console/page errors.
- **Major correction (styling rebuild):** user flagged that the previous pass invented a parallel `.agency-*`/`.app-*` CSS component system instead of reusing the purchased "light" template's real markup — "you have broken the styles entirely... just look at the light css and jss and just the colors that will change." Rebuilt every public page to use the template's actual classes/DOM structure (theming stays confined to color overrides in `app-blue.css`): layout (`.wrapper`/`.content`/`.height-emulator`/real `.main-footer` footer-widget columns), Home (`.hero-section` Swiper hero, `.listing-grid`+Isotope category filters, `.city-carousel` communities, `.about-wrap`/`.services-opions` why-us, `.inline-facts` stats, `.clients-carousel` partner banks, `.testimonilas-carousel` testimonials, `.post-item` blog cards, `.api-wrap` closing CTA), Listings index/show (`.geodir-category-listing` cards via new `x-property-card` component, real filter sidebar, `.fw-carousel` gallery, `.ps-facts-wrapper`, `.nerby-list-wrap`, Leaflet map instead of the template's Google-Maps-keyed `map-single.js`), About (`.agnet-carousel`/`.agent-card-item`, `.inline-facts`), Team index/show (`.agent-card-item` grid, `.agent-preofile-*` profile), Services index/show (`.contacts-card-item`, `.process-wrap`, `.accordion`), FAQ (`.help-item-wrap`/`.accordion`), Contact (`.contacts-cards-wrap`/`.contacts-form-wrap` + Leaflet), Blog index/show (`.post-items`/`.post-item`, `.category-widget`), Site Visit + Mortgage forms (`.custom-form`/`.cs-intputwrap`). Deleted the old `x-listing-card` component and the invented CSS block from `app-blue.css` (now color-overrides only). Left `resources/views/parcels/**` and `resources/views/admin/**` alone (no template equivalent exists for the land-parcel GIS feature or the admin CRUD; their preexisting `.app-table`/`.app-stat-grid`/`.parcel-*` utility classes predate this correction and weren't part of the complaint).
- Caught and fixed 3 real template gotchas along the way via Playwright browser checks (not just curl): missing `.loader-wrap` (page stuck at `opacity:0` forever), missing `.limit-box` marker (crashed `scrollToFixed` sticky sidebar with `Cannot read properties of undefined (reading 'top')` on the listing detail page), and reusing the `.num`/`.stats` counter-widget classes for the mortgage calculator's live result (the template's global `countTo()` scroll-appear handler hijacked and zeroed it out — moved to `.property-single-header-price` instead). Full regression pass across all 16 rebuilt pages: 200 status + zero console/page errors on every one.
- See memory `feedback_reuse_light_template_markup` for the durable rule going forward: always grep the matching page in `light/*.html` for real classes/structure first; never invent new CSS.
- **Home rebalanced toward "agency" feel, nav Properties → Services:** user wanted Home to read more like a corporate agency site with small/condensed property-browsing sections rather than being dominated by listing grids, and wanted the top nav's "Properties" dropdown replaced with "Services". Changes: collapsed Home's 4 separate listing-grid sections (Featured Portfolio, Browse Latest by Category, Recently Sold, Investment Opportunities) down to a single compact "Explore Properties" grid (6 items, category filter tabs kept) followed by a new "Our Services" section (4 `.contacts-card-item` cards + link to full services page) — moves the page's center of gravity from listings to the agency's services/expertise; PageController@home trimmed to match (dropped unused `categories`/`recentlySold`/`investmentOpportunities`/`featuredParcels`/`team` query results that the view no longer renders). Nav: swapped the "Properties" dropdown for a "Services" dropdown (sourced from a new `View::composer('layouts.app', ...)` in AppServiceProvider sharing `$navServices` on every page, since the layout is shared app-wide); "Company" dropdown lost its now-redundant Services link (About/Our Team/FAQ only). Investment Opportunities and Land Parcels links, no longer in the nav, were added to the footer's "Helpful links" column so they stay reachable; the header's "Explore Properties" CTA button is still the primary path into listings. Full 16-page Playwright regression re-run afterward: all still 200 + zero console/page errors.
- Copied the About page's story section (`.boxed-content > .about-wrap` — title/body/checklist + image, sourced from the same `$about` SiteContent already passed to Home) into Home right after the hero, before Explore Properties, reinforcing the agency-first feel from the previous change.
- **Real input widgets, not plain selects:** user flagged that some form inputs weren't reusing the "already made" input components in the light template. Audited every `<select>`/price/quantity field across Home hero search, Listings filter sidebar, Site Visit form, and Mortgage form. Replaced plain `<select>` dropdowns with the template's real `chosen-select` (niceSelect-styled) dropdowns; replaced "up to GHS X" preset price `<select>`s with the template's real dual-handle `price-range-double` (ionRangeSlider) slider — its value serializes as `"min;max"` on one `price_range` field, parsed server-side in `ListingController::index()` via `explode(';', ...)` (kept `min_price`/`max_price` as a fallback path); replaced the Bedrooms preset-number `<select>` on the listings filter with the template's real `.quantity_wrap` +/- spinner. None of these needed new JS — scripts.js already auto-initializes `.chosen-select`/`.price-range-double`/`.quantity-item` globally; only had to add the right classes/data-attributes and correct form `name`s. Verified via Playwright (`.nice-select`/`.irs`/`.quantity-item` element counts + screenshots) and via curl that server-side filtering actually works with the new `price_range` format. Full 16-page regression re-run clean again afterward.
- **Logo vertical centering fix:** the base template's `.logo-holder` CSS uses a `position:relative; top:50%; margin-top:-6px` trick tuned for the original 12px-tall image logo; when we swapped it for a text wordmark ("BlueGate Realty") earlier, the override reset `top`/`margin-top` to 0 without restoring proper centering, leaving the logo sitting at the top of the 90px header instead of centered next to the nav items. Replaced the magic-number hack with `display:flex; align-items:center; height:100%` on `.logo-holder`, which centers correctly regardless of font metrics and adapts automatically at the mobile breakpoint (where `.header-inner` height changes to 80px) instead of needing a second hardcoded offset. Verified via Playwright bounding-box check (logo box exactly matches header box top/bottom at both 1440px and 600px viewports) plus screenshots.
- Removed the "From the Blog" section (sat right after Testimonials) from Home per user request. Dropped the now-unused `latestPosts`/`BlogPost` query from `PageController@home` too. Blog is still fully reachable via the Resources nav dropdown and its own `/blog` index/show pages — only the home-page teaser is gone. Confirmed via Playwright the section no longer appears and the page still renders clean.
- **Footer column border fix:** user flagged an ugly div border/box look in the footer. Root cause: a leftover shared selector rule in `app-blue.css` (`.main-footer, .footer-widget, .sub-footer, .modal_main, ...`) applied a `1px solid` border + drop-shadow to *every* `.footer-widget` column individually — meant for modal/sidebar panels, not footer columns, so each footer column (BlueGate Realty / Helpful Links / Our Contacts / Get Started) rendered as its own bordered box instead of blending into the navy footer background. Removed `.footer-widget` from that selector list; `.main-footer`'s navy background (and the intentional `.footer-widget-title` underline + `.footer-bottom:before/:after` center divider, both real template styles) are untouched. Verified via screenshot — columns now blend cleanly.
- **Major perf fix — app was hitting a remote Supabase Postgres in `eu-west-1` for every request:** user asked for the app to be "very fast." Investigation found `.env` `DB_HOST` pointed at `aws-0-eu-west-1.pooler.supabase.com` (a PgBouncer pooler, hence the `DB_EMULATE_PREPARES=true` workaround) — this predates my involvement (log history shows earlier failed connection attempts to this same host before this session). Measured directly: **1.4s just to open a DB connection**, ~200-300ms per query, and full page loads of **3-12 seconds**. Since `SESSION_DRIVER`/`CACHE_STORE`/`QUEUE_CONNECTION` are all `database`, *every* request paid this cost multiple times over. Fix: created a fresh local Postgres 17 database `bluegate_realty` (found a local Postgres already running via Homebrew; there was also a pre-existing local `agency_dev` DB but its schema — PascalCase tables, Prisma-style `_JoinTable` naming — belongs to the unrelated abandoned `light-next` Next.js attempt, not this app, so left untouched), repointed `.env` at `127.0.0.1:5432`, dropped the now-unneeded `DB_SSLMODE`/`DB_EMULATE_PREPARES`, and re-ran `migrate` + `db:seed` fresh. Result: DB connect dropped to ~35ms, queries to ~18ms, full page loads to **40-75ms** — roughly 60-100x faster. Also fixed a real N+1 while in there: none of `PageController@home`, `ListingController@index/show`, or `team/show.blade.php`'s listings query eager-loaded the `agent` relation that `<x-property-card>` reads for every card (12 queries for 6 listings → 2 with `->with('agent')`); added indexes on `property_listings` (`is_published`, `property_type`, `type`, `city`, `status`, `is_featured`, `is_investment`, `is_new`, `community_id`, `team_member_id`), `parcels.status`, and `blog_posts` (`is_published`, `category`) since none of the filtered/sorted columns had one beyond the primary key. Full 16-page regression re-run clean on the new DB. **Note for future sessions:** local dev DB is now `bluegate_realty` on `127.0.0.1:5432` (Homebrew Postgres 17, user `knight`, no password) — do not repoint `.env` back at the Supabase host without discussing with the user first, since that was the entire root cause of the slowness.
- Next up: item 2, About Us needs real content depth (mission/vision/values/history/timeline/awards/certs/licenses/management message/CSR/partners) — current About page has story+stats+team but not the full spec; PLAN.md item 2 should stay `[~]` until that's built out.
- **Listing photos now upload to real object storage (Cloudflare R2, not Supabase):** user initially asked for Supabase Storage bucket `listings`; after credential back-and-forth it turned out to be Cloudflare R2 (S3-compatible), account `e2d444c5844e257790db439638e88ef4`, bucket `agency-listings`, public via `pub-380d4e01c05d4d22bbf56529e2afabc9.r2.dev`. Added `league/flysystem-aws-s3-v3`, a new `r2` disk in `config/filesystems.php` (region `auto`, path-style endpoint), and `R2_*` env vars (real values in `.env`, placeholders in `.env.example`). `AdminListingController` now accepts real `images[]` file uploads (stored to `listings/` on the `r2` disk, merged with existing `image_paths`) plus `remove_images[]` checkboxes to delete individual photos, replacing the old "paste newline-separated paths" textarea. `PropertyListing::primaryImage()`/`image_paths` needed no changes since Laravel's `asset()` helper passes absolute URLs through unchanged. Verified end-to-end with Playwright: uploaded a real file through the admin form, confirmed it round-tripped through R2 and rendered as a thumbnail with a working Remove checkbox, then cleaned up the test image from both the DB and the bucket. Team member and site-content image fields (`AdminTeamMemberController`, `AdminContentController`) still take plain text paths/URLs — not converted, wasn't in scope.
- **Note for future sessions:** local dev DB is still local Postgres (`bluegate_realty`) — the Supabase Postgres pooler credentials the user pasted mid-session were for the old (slow) DB setup, not storage, and were *not* applied to `.env`. Don't repoint `DB_HOST` at Supabase without explicit confirmation.
- **Production-hardening pass (code-level only — see below for what still needs the user):** added `throttle:5,1` to `POST /login` (previously unlimited brute-force attempts against the single admin account); `AppServiceProvider::boot()` now calls `URL::forceScheme('https')` when `APP_ENV=production`; `bootstrap/app.php` now calls `$middleware->trustProxies(at: '*')` so client IP/HTTPS detection works correctly behind a load balancer or CDN; added `SESSION_SECURE_COOKIE` (false locally, must be true in prod) to both `.env` and `.env.example`; added a "PRODUCTION CHECKLIST" comment block at the top of `.env.example` plus inline warnings on `MAIL_MAILER=log` (currently a no-op — contact/site-visit/mortgage form notifications go nowhere) and the DB section. Verified via `php artisan route:list -v` that the throttle middleware is actually attached, and a full smoke test (home/login both 200, no boot errors from the new provider/bootstrap code).
- **Login page polish + navbar cleanup:** removed the public "Sign In" link from the header nav (kept the Logout button for authenticated admins) — this is a backend-only admin login, not meant to be advertised to public site visitors, matching the original (pre-session) intent. Also found and fixed a real bug in the login page's CSS: the inline `style="padding:150px..."` I'd used always won over the template's own mobile media queries (inline beats stylesheet regardless of breakpoint), so the page ignored the template's responsive rules below 1068px. Replaced it with a proper `@push('styles')` block using a `.login-page` class with higher specificity than the template's rules at every breakpoint the template defines, so mobile/tablet layouts now behave correctly. Along the way, added a small lock icon next to the heading (first attempt used a 46px circular badge that grew `.mr_title`'s content enough to collide with the template's absolutely-positioned "Have a question?" label — switched to a small inline icon before the heading text instead, no collision). Also discovered and fixed a real pre-existing template layout bug (not introduced this session, but only exercised now that this component is actually used): `.main-register_box` has no explicit width, so as a `display:flex` child its width is intrinsic/ambiguous, which made `.main-register-holder`'s `width: min(100% - 25px, 950px)` resolve against that ambiguous size and collapse the entire card to ~280px at intermediate (tablet) viewport widths. Fixed by pinning `.main-register_box { width: 100% }` scoped to `.login-page`. Verified all three breakpoints (1440px, 820px, 390px) via Playwright screenshots plus a full login round-trip, zero console errors.
- **Built out a real admin dashboard + navigation system — previously the whole admin area was a single bare page reusing invented `.app-*` classes with zero shared chrome across CRUD screens.** Root cause found via research: the licensed template ships a *complete*, never-before-used dashboard component system — `public/light/css/db-style.css`, `light/dashboard.html` (KPI cards, chart, activity feed, sidebar), `light/dashboard-requests.html` (`.bookings-item` lead cards), `public/light/js/charts.js` (Chart.js 2.x, bundled but unused) — none of it was wired into the Laravel app before now. Built:
  - `resources/views/layouts/admin.blade.php`: shared admin shell (extends `layouts.app`) with the real `.user-dasboard-menu_wrap` sidebar (active-state highlighting via `request()->routeIs()`, a live badge count of new leads via a `View::composer('layouts.admin', ...)` in `AppServiceProvider`), `.dashboard-title` toolbar, and the real `.boxed-container`/`.row`/`.limit-box` structure. Loads `db-style.css` + `db-scripts.js` only on admin pages.
  - Retrofitted all 12 existing admin CRUD views (listings/parcels/services/team/faqs/content, index+form each) from a duplicated ad hoc wrapper onto this shared layout — sidebar nav is now consistent everywhere. Also fixed leftover "Rentstate" branding in the parcels form/index page titles, found along the way.
  - Rebuilt `/admin/dashboard` for real: `.db-single-facts` KPI cards (listings/published/parcels/new leads (30d)/revenue/team), a live weekly-leads bar chart (Chart.js, real query data, BlueGate blue instead of the template's demo orange), and a merged recent-activity feed across inquiries/site visits/mortgage applications/orders. The chart required trimming `public/light/js/charts.js` — the file bundled the actual Chart.js library *plus* a hardcoded demo chart init appended at the very end targeting the same `#canvas-chart` id; left in place it would have fought a real per-page init for the same canvas. Trimmed the file to the library only (kept the reusable `legendClickCallback` helper), then added a small inline per-page init script with real Blade-supplied JSON data.
  - **Two brand-new admin pages exposing data that had zero admin UI before this session**, despite the underlying tables already existing and collecting real user submissions: `/admin/requests` (Inquiry + SiteVisit + MortgageApplication merged into one list via the template's real `.bookings-item` lead cards, with call/email/delete actions — delete verified working end-to-end via Playwright) and `/admin/orders` (read-only Paystack payments ledger via `.app-table`, consistent with the existing precedent that financial/tabular admin data doesn't have a template equivalent). Added an `Inquiry::listing()` relation that didn't exist yet, needed for the merged feed.
  - Seeded realistic demo data for all four of these previously-empty tables (6 inquiries, 5 site visits, 4 mortgage applications, 5 orders spread over the last ~30 days) via a new `DatabaseSeeder::seedLeadsAndOrders()` method, since a from-scratch admin panel with all zeros doesn't actually demonstrate anything. Note: the *existing* top-level `db:seed` has a pre-existing idempotency bug unrelated to this work (`PropertyListing::updateOrCreate` collides on the `property_code` unique constraint on a second run) — didn't fix it since out of scope, but it means `seedLeadsAndOrders()` currently needs to be invoked directly (via tinker/reflection) rather than through a full `db:seed` re-run on an already-seeded DB.
  - Two real bugs caught only because this component was actually exercised for the first time: (1) missing `.limit-box` marker crashed `scrollToFixed` on every admin page (`Cannot read properties of undefined (reading 'top')`) — same class of gotcha noted in an earlier session for the listing detail page, same fix; (2) initial `.db-date`/`.tfp-det-container` slot (sized for a short date string) badly wrapped/collided when misused for a longer stats sentence on the new Requests/Payments pages — replaced with the existing `.app-stat-grid` chip row pattern instead.
  - Full Playwright regression: all 14 admin routes (dashboard, 6×index, 6×create, requests, orders) return 200 with the sidebar present and zero console errors; chart renders with a working legend; a real request delete round-tripped (15→14, flash message rendered).
- **Real production blockers that need the user's decisions, not code changes:** (1) DB is local Postgres on the developer's laptop — unreachable from any real host, needs a provisioned managed Postgres instance; (2) `MAIL_MAILER=log` — no real mail is sent; (3) `PAYSTACK_PUBLIC_KEY`/`PAYSTACK_SECRET_KEY` and `ARKESEL_API_KEY` are empty — payments and SMS won't work; (4) no hosting platform chosen yet, so no deploy pipeline/process supervisor for the queue worker exists; (5) `APP_URL`/domain not decided. `FILESYSTEM_DISK` was deliberately left as `local` (not switched to `r2`) since nothing but listing photo uploads uses object storage — flagged rather than silently changed.
- **Rebuilt `/login` (admin sign-in) using real template markup:** the previous login page (`resources/views/auth/login.blade.php`) used an invented `.auth-*` CSS system (same category of violation as the earlier "Major correction" — see [[feedback_reuse_light_template_markup]]), even though `app-blue.css` already had blue-theme overrides prepared for the light template's actual login/register component (`.main-register-container` etc., found live in `light/index.html:1841-1999`, a tabbed Login/Register modal). Rebuilt the page using that real component's classes, adapted from a JS-toggled modal to a static standalone page (dropped the tabs-menu, close button, and non-functional demo-only Register tab / social login / "lost password" links since none of those are backed by real routes yet — avoids half-finished UI). Added a "Sign In" link (using the template's real `.show-reg-form` class, no JS binding conflict since that class alone isn't wired to any modal-toggle handler) to the header nav via `layouts/app.blade.php`, plus a real Logout button when authenticated. Verified via Playwright: renders with zero console errors, real login flow works, footer's `position:fixed` (an intentional template trick, not a bug — see `.height-emulator` spacer in `style.css:3628-3649`) renders fine at normal viewport size despite looking broken in a fullPage screenshot.
